[project]
name = "hagiographies"
version = "0.1.0"
description = "Hagiographies project utilities"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "dotenv>=0.9.9",
]

[tool.uv.workspace]
members = ["utils/importer", "utils/utilities", "utils/documenter", "utils/exporter"]
