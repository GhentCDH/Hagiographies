# PARKED — the pre-rewrite importer (cli.py) and IIIF tool (iiif_manifests.py),
# kept for reference. They target the old 20-table schema in
# utilities.legacy_model and cannot run against the current database schema.
