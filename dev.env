PG_DATABASE_URL=postgresql+psycopg://hagiographies:changeme@postgres:5432/hagiographies
POSTGRES_USER=hagiographies
POSTGRES_PASSWORD=changeme
POSTGRES_DB=hagiographies
POSTGRES_PORT=5432
POSTGRES_HOST=postgres

# ── Mathesar ──
# Stable Django secret so saved DB-connection passwords survive restarts.
SECRET_KEY=QKaUisCWjVSZzRfsELcxvZgx3WuTbZBZFxBRCNT2pnx7A6mbK0

# RPC client config (mathesar-tools). URL is the Docker service name; use
# http://localhost:8000 when running the script from the host.
MATHESAR_URL=http://mathesar:8000
MATHESAR_USERNAME=admin
MATHESAR_PASSWORD=admin12345
MATHESAR_DATABASE_ID=1
# Schema is resolved by name on every run (its oid changes on drop + reimport).
MATHESAR_SCHEMA=public